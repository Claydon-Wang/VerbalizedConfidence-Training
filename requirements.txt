# Derived from requirements.txt for Qwen/Qwen3-1.7B training.
# Keep the original requirements.txt untouched; this file updates the
# packages most likely to matter for Qwen3 compatibility.
#
# Key changes vs requirements.txt:
# - transformers: Qwen3 requires >= 4.51.0
# - vllm: upgraded to 0.11.1 to support torch 2.9
# - torch: 2.9.0 (required by vllm 0.11.1)
# - trl / peft: required by this training codebase but missing from the old file
# - tokenizers / huggingface-hub: aligned with newer transformers
# - accelerate: trl 0.23.1 requires >=1.4.0

# --- Core training ---
torch==2.9.0
torchaudio==2.9.0
torchvision==0.24.0
transformers==4.56.1
tokenizers>=0.21.0,<1.0
huggingface-hub>=0.34.0,<1.0
accelerate==1.4.0
trl==0.23.1
peft==0.14.0
datasets==4.0.0
deepspeed==0.17.0
evaluate==0.4.3

# --- Inference ---
vllm==0.11.1

# --- Logging / experiment tracking ---
wandb==0.19.6
swanlab==0.7.11

# --- Utilities ---
numpy==1.26.4
pandas==2.2.3
safetensors==0.5.3
sentencepiece==0.2.0
tiktoken==0.9.0
pyyaml==6.0.2
tqdm==4.67.1
packaging==25.0
regex==2024.11.6
sympy==1.13.3
scipy
einops==0.8.1
matplotlib
jupyterlab
pytest==8.4.0
python-dotenv==1.1.0
math-verify